provider "proxmox" {
  pm_api_url      = "https://${var.proxmox_host}:8006/api2/json" # Адрес API Proxmox VE
  pm_user         = var.proxmox_user
  pm_password     = var.proxmox_password
  pm_tls_insecure = true # Отключение проверки TLS-сертификата
  pm_timeout      = 600
}
